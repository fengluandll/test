/**
 * Created by Allbts-IT on 24/06/2016.
 */
'use strict';

    var a = [10,8,6];
    var target = 11;
    for (var i = 0 ; i < a.length ; i++) {
        if (a[i] == target) break;
    }
console.log('The index is ' + i);